from django.apps import AppConfig


class ExtractionCoreConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.extraction_core"
    verbose_name = "Extraction Core"
