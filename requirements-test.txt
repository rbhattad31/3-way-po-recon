# Core Django stack
Django>=4.2,<5.0
djangorestframework>=3.14
django-filter>=23.0
django-celery-results>=2.5

# Database
# mysqlclient>=2.1          # needed for production MySQL — not needed for test (SQLite used)

# Matching
thefuzz>=0.22
python-Levenshtein>=0.21    # speeds up thefuzz
rapidfuzz>=3.0

# Utilities
python-dotenv>=1.0
dateparser>=1.1
celery>=5.3
redis>=5.0

# AI (stubs needed so imports don't fail — actual calls are mocked in tests)
openai>=1.0
langchain>=0.1
langchain-openai>=0.1

# Testing
pytest>=8.0
pytest-django>=4.8
factory-boy>=3.3
pytest-cov>=5.0             # coverage reports
